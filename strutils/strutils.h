#ifndef CRAWON_STRUTILS_H
#define CRAWON_STRUTILS_H

#include "../lexer.h"
#include "../parser.h"
#include "../interpreter.h"

RwnObj *rwn_split(Interpreter *context, RwnObj **args);

#endif //CRAWON_STRUTILS_H
